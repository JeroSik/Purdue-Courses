#include <bits/stdc++.h>
#include <iostream>
#include <stdio.h>
#include <tuple>
#include <utility>

using namespace std;

int main() {
    int t, price, n, val;
    scanf("%d", &t);

    while (t-- > 0) {
        scanf("d", &price);
        scanf("d", &n);
        int demon[n];

        for (int i = 0; i < n; i++) {
            scanf("d", &val);
            demon[j] = val;
        }

        // Start dp
        int dp[price + 1];
        for (int i = 0; i < n; i++) {
            scanf("d", &val);
            demon[j] = val;
        }
    }

    return 0;
}