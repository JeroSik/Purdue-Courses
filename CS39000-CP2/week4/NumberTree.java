import java.util.*;
import java.lang.*;

public class NumberTree {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);

        String input = scan.nextLine();
        int H = Character.getNumericValue(input.charAt(0));

        for (int i = 2; i < input.length(); i++) {

        }
    }
}